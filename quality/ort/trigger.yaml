tool: ort
language: typescript
language_version: "18-22"
technique: "License & Dependency Review"
target_files: [../../src/service.ts]
run:
  command: "npx cdxgen -t js -o ort-sbom.json --install-deps false"
expects:
  verified: true
verified_in_session: true
verified_result: "tool verified against TypeScript proof suite"
